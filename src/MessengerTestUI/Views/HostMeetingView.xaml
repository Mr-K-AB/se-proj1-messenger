<UserControl x:Class="MessengerTestUI.Views.HostMeetingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MessengerTestUI.Views"
             xmlns:views="clr-namespace:MessengerTestUI.Views"
             xmlns:viewmodels="clr-namespace:MessengerTestUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Navigation Bar -->
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewmodels:DashboardServerViewModel}">
                <views:DashboardServerControl/>
            </DataTemplate>
        </Grid.Resources>
        <Menu>
            <MenuItem Header="Dashboard" Command="{Binding NavigateServerDashboardCommand}"/>
            <MenuItem Header="Whiteboard"/>
            <MenuItem Header="Screenshare"/>
            <TextBlock Text="{Binding IP}"></TextBlock>
            <TextBlock Text="{Binding Port}"></TextBlock>
        </Menu>

        <!-- Profile Information -->
        <Button Content="End Meeting" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,10,0"/>

        <!-- Content Control for Dynamic Content -->
        <ContentControl Content="{Binding SubViewModel}" Margin="0,25,0,0"/>


    </Grid>
</UserControl>
