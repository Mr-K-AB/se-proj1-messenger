<UserControl x:Class="MessengerApp.Views.HostMeetingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessengerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Navigation Bar -->
        <Menu>
            <MenuItem Header="Dashboard" Click="Dashboard_Click"/>
            <MenuItem Header="Whiteboard" Click="WhiteBoard_Click"/>
            <MenuItem Header="Screenshare" Click="HostScreenshare_Click"/>
            <MenuItem Header="Chat" Click="Chat_Click"/>
            <TextBlock Text="{Binding IP}"></TextBlock>
            <TextBlock Text="{Binding Port}"></TextBlock>
        </Menu>
        <Grid x:Name="OverlayPanel" Panel.ZIndex="1000" Visibility="Collapsed" HorizontalAlignment="Right" Width="300" Background="AliceBlue">
            <ContentControl x:Name="OverlayContent"/>
        </Grid>
        <!-- Profile Information -->
        <Button Content="End Meeting" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,10,0"/>

        <!-- Content Control for Dynamic Content -->
        <ContentControl x:Name="MainContent" Margin="0,25,0,0"/>

    </Grid>
</UserControl>
